# gaiax-populateBD-keywords

Use this to populate Databases with service offerings information and keywords.

For execution use:

1. pip install poetry (or similar)
2. poetry install
3. poetry run python main.py